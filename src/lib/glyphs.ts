/**
 * The ★ from the source markdown. U+FE0E (variation selector-15) forces the
 * text presentation — without it phones substitute a colour emoji star, which
 * breaks the design's flat gold/pink treatment.
 */
export const STAR = '★︎';
